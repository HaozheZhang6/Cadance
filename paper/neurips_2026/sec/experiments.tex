\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
We evaluate 10+ models with non-uniform per-task coverage --- the most expensive frontier reasoning systems are scored on every task, while smaller open-source baselines are scored on the subset of tasks for which results are most informative. The headline figures (Figures~\ref{fig:main_results}, \ref{fig:capability_profile}) report the seven frontier proprietary systems with full four-task coverage: \textbf{GPT-4o}, \textbf{GPT-5.3} (thinking and non-thinking), \textbf{Claude Opus 4.7} (thinking and non-thinking), and \textbf{Gemini 3.1 Pro} (thinking and non-thinking). Additional models scored on a subset of tasks include open-source vision-language baselines (\textbf{Qwen2.5-VL} at 7B/32B/72B and \textbf{InternVL3} at 1B/8B/38B/78B for image-conditioned tasks) and the CAD-specialised \textbf{cadrille-RL} for transfer evaluation (\S\ref{sec:transfer}). All models receive identical prompts; we report decoding temperature 0 and a 60\,s per-sample wall-clock budget. Inference is run on an internal cluster of 8$\times$A800-80GB nodes for open models and via official API endpoints for proprietary models. We additionally include a \emph{blind baseline} (a frequency-prior predictor that ignores inputs) and an \emph{Oracle} reference (a model with parameter-substitution access to the ground-truth code, which trivially achieves 100\%). Headline results use BenchCAD-Lite (530 stratified samples); Appendix~\ref{app:full_results} reports per-model per-task coverage and the full 17{,}900 set for the top systems.

\subsection{Main Results}
\label{sec:exp_main}

Table~\ref{tab:main_eval} reports per-task accuracy and the macro-mean for all evaluated models. The strongest model overall is GPT-5.3 (think) at 46.8\% Avg --- well below the IoU $\geq 0.99$ verification threshold required to enter the BenchCAD ground-truth split, and leaving substantial room for improvement on every task category. Three observations stand out.

\paragraph{Absolute scores remain low.} Even the top reasoning models score below 50\% on every task; the strongest open-source VLM (Qwen2.5-VL-72B, 23.5\% Avg) trails the best proprietary system by 23 points. CAD-specialised models (CAD-Coder-7B, CAD-Recode-1.5B) operate only on text or point-cloud inputs and so cannot be evaluated on the image-conditioned tasks; on their applicable tasks they sit between mid-tier proprietary and top-tier open-source models, despite matching the latter on the original DeepCAD benchmark.

\paragraph{Scaling is not enough.} Within the InternVL3 family, scaling from 1B to 78B raises Avg from 7.2\% to 21.7\% --- a 14.5-point gain over a 78$\times$ parameter expansion. The Qwen2.5-VL family shows an even shallower slope: 7B to 72B yields only 7.5 points. Both contrast with the 23-point jump from Qwen2.5-VL-72B to GPT-5.3 (think). Reasoning capability and post-training quality dominate scale within open-source families, but neither saturates the benchmark.

\paragraph{The geometry-program decoupling.} Several mid-tier models (Gemini 3.1 Pro, GPT-4o) achieve respectable IoU on \texttt{img2cq} but mid-tier scores on \texttt{qa\_img}, indicating that they recover the outer shape but cannot answer dimensional questions about it. The reverse pattern --- high \texttt{qa} but low \texttt{img2cq} --- does not appear, consistent with QA being strictly easier than full code synthesis.

\input{tables/tab_main_eval}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_3.png}
  \caption{\textbf{Per-model performance across the four BenchCAD task categories} (frontier proprietary subset; open-source baselines reported in Table~\ref{tab:main_eval}). Bar style encodes the task (CodeGen IoU, CodeEdit, QA-img, QA-code); colour encodes model family (GPT in green, Claude in orange, Gemini in purple); within-family variants distinguish thinking vs.\ non-thinking modes. Two patterns stand out and motivate the diagnostics that follow: (i) the QA-img bar is consistently lower than QA-code for every model (the \emph{Vision Bottleneck}, \S\ref{sec:exp_capability}), and (ii) the CodeEdit bar trails CodeGen on every model despite editing being intuitively easier than greenfield generation, reflecting the \emph{Edit Leakage} effect (\S\ref{sec:exp_edit}).}
  \label{fig:main_results}
\end{figure*}

\subsection{The Vision Bottleneck and Capability Profile}
\label{sec:exp_capability}

The capability-decomposition framework of \S\ref{sec:capability} predicts that the gap between \texttt{qa\_img} and \texttt{qa\_code} on matched prompts isolates the cost of visual perception ($C_1$) from parametric abstraction ($C_2$). Table~\ref{tab:main_eval} shows that this gap is large and remarkably consistent: 19.6\,pt for GPT-5.3, 19.8\,pt for Claude Opus 4.7, 16.9\,pt for GPT-4o, 12.9\,pt for Qwen2.5-VL-72B, and 4.1\,pt for InternVL3-1B. We name this systematic deficit the \textbf{Vision Bottleneck}.

The Vision Bottleneck has three structural properties that distinguish it from a generic perception-versus-reasoning trade-off:
\begin{itemize}[leftmargin=1.2em,topsep=2pt,itemsep=1pt]
  \item \emph{It survives chain-of-thought.} Reasoning variants (GPT-5.3 think, Claude Opus 4.7 think, o3 high) show the largest absolute gap, not the smallest --- additional reasoning tokens improve $C_2$ on \texttt{qa\_code} faster than they improve $C_1$ on \texttt{qa\_img}.
  \item \emph{It survives view-count augmentation.} Doubling from four to eight canonical views (Appendix~\ref{app:ablation}) recovers $\leq$1.5\,pt of the gap, far below the 15--20\,pt that a perception bottleneck would close.
  \item \emph{It survives parameter scaling within open-source families.} The gap shrinks toward 0 only because absolute scores collapse toward the random baseline at small scales; the relative gap is preserved.
\end{itemize}

These properties suggest that the Vision Bottleneck is a property of the \emph{representation interface} between vision encoders and language decoders, not of any single model's capacity. Closing it likely requires training procedures aware of the parametric-symbolic structure of CAD geometry --- e.g.\ vision pretraining objectives that predict parametric properties, not just dense features.

\paragraph{Per-model capability profile.} Beyond the aggregate Vision Bottleneck, Figure~\ref{fig:capability_profile} (left) decomposes each model's score along six axes spanning surface similarity, local feature accuracy, global geometry, CAD understanding, CAD code robustness, and synthesis. The radar reveals that frontier models occupy structurally different capability shapes: GPT-5.3 (think) leads on CAD understanding and code robustness; Claude Opus 4.7 (think) leads on local feature accuracy and synthesis; Gemini 3.1 Pro (think) leads on global geometry. No single model dominates all axes, and strength on one axis often coincides with relative weakness on another --- consistent with each frontier system optimising different points along the parametric pyramid rather than uniformly closing the gap.

\paragraph{Qualitative failure modes.} Figure~\ref{fig:capability_profile} (right) shows four failure modes that the aggregate scores compress. \emph{No industry understanding}: a coil spring rendered as a stack of disks because the model never produced a helical sweep, despite the input clearly showing helical winding. \emph{Wrong plane}: a part with all the correct features but constructed on the wrong base workplane, leading to a 90$^\circ$-rotated solid that the rotation-invariant IoU still penalises in some axes. \emph{Wrong ops}: a bracket whose target geometry required a chamfer plus a Boolean cut, but the model produced only a single extrude --- the model identified the silhouette but not the operation sequence. \emph{No detail}: a bolt that omits thread, fillet, and head detail expected by DIN/ISO standards. These four modes recur across families and account for the bulk of hard-tier failures.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_4.png}
  \caption{\textbf{Capability profile and qualitative failure modes} (frontier proprietary subset). \emph{Left:} per-model capability radar across six axes --- surface similarity, local feature accuracy, global geometry, CAD understanding, CAD code robustness, and a sixth synthesis axis --- showing that no model dominates on all dimensions and that strengthen-on-one often coincides with weaken-on-another. \emph{Right:} four representative failure modes elicited by BenchCAD's hard tier on the strongest reasoning models, illustrating concrete capability deficits that the aggregate scores in Table~\ref{tab:main_eval} compress: \emph{(i) no industry understanding} --- a coil spring that lacks the helical sweep entirely; \emph{(ii) wrong plane} --- a part with the correct features placed on the wrong base workplane; \emph{(iii) wrong ops} --- a bracket whose target was a chamfer + boolean cut but the model emitted only a single extrude; \emph{(iv) no detail} --- a bolt that omits thread, fillet, and head detail expected by the standard.}
  \label{fig:capability_profile}
\end{figure*}

\subsection{The Family Cliff}
\label{sec:exp_cliff}

Table~\ref{tab:family_cliff} stratifies \texttt{img2cq} pass rate by family difficulty tier. The strongest model collapses by 48\,pt from easy (68\%) to hard (20\%), exhibiting a \textbf{Family Cliff} that we attribute to two interacting factors: (i) hard-tier families exercise advanced operations (helical sweeps, twist-extrusion, lofted Booleans) that scarcely appear in any prior CAD training corpus; and (ii) hard-tier families enforce non-trivial parametric constraints (gear module--tooth--pitch consistency, spring free-length--turn-count coupling) that no model evaluated reliably preserves.

The Family Cliff persists across architectures. Even within easy-tier families, the best open-source VLM (Qwen2.5-VL-72B at 38\%) trails GPT-5.3 by 30\,pt, indicating that closed-source post-training quality is the dominant factor at low difficulty; on hard-tier families the same gap shrinks to 15\,pt, because both models hover near the floor.

\input{tables/tab_family_cliff}

\subsection{The Edit Task: Verified Parametric Editing}
\label{sec:exp_edit}

Editing is the dominant industrial use case for CAD code generation: practising engineers spend the majority of CAD work modifying existing parametric models rather than creating greenfield ones. Yet no prior public benchmark provides verified before/after CadQuery pairs with both target-success \emph{and} program-wide preservation metrics. BenchCAD-Edit closes this gap with 748 curated edit pairs balanced across 106 families and four edit categories (Table~\ref{tab:edit_leakage}, top).

\paragraph{Edit Leakage.} We define \textbf{Edit Leakage} as the failure mode where a model applies the requested change correctly but silently corrupts an unrelated parametric feature: enlarging a gear bore while accidentally changing the tooth count, adding a chamfer while shifting a hole's centre, removing one spoke while breaking the hub geometry. Disaggregating Pass@1 into target-success and non-target-preservation reveals that even the strongest reasoning model achieves target success on 84\% of edits but full Pass@1 on only 31\%: \textbf{64\% of nominally successful edits exhibit Edit Leakage}. This locality failure is invisible to any prior CAD eval that scores only output IoU against a ground-truth target.

\paragraph{Multi-step collapse.} Edit categories exhibit a sharp difficulty gradient. Dimensional edits (change a single parameter value) achieve $\sim$70\% pass for top reasoning models. Additive edits (add a chamfer, hole, or fillet) drop to $\sim$40\%. Subtractive edits (remove one of $N$ symmetric features) drop to $\sim$30\%. Multi-step edits requiring parameter-dependency reasoning (\emph{``increase tooth count to 30 while preserving the pitch diameter''}) collapse to \textbf{under 15\% across every model evaluated}, including GPT-5.3 and Claude Opus 4.7. This single number quantifies the gap between current MLLM editing capability and industrial usefulness.

\paragraph{Vision Bottleneck on edit.} The \texttt{edit\_img} versus \texttt{edit\_code} contrast reproduces the Vision Bottleneck pattern of \S\ref{sec:exp_capability}: GPT-5.3 scores 52.4\% on text-only edits but 31.0\% on image-conditioned edits --- a 21.4\,pt gap, slightly larger than the QA-task gap. The bottleneck is preserved across reasoning and non-reasoning models, confirming that visual perception is a cross-task systematic deficit rather than a property of any single task.

\paragraph{Diff bloat.} Beyond pass rate, we report the average \emph{diff bloat ratio}: lines changed by the model divided by the minimal correct diff. Even on dimensional edits where the minimal diff is one line, GPT-5.3 averages 7.8 changed lines, frequently re-emitting the entire program rather than patching the target line. Models edit by rewriting, not by patching --- a property absent from human engineering practice and a barrier to using these models in version-controlled engineering workflows.

\paragraph{Scoring methodology, edit examples, and leaderboard.} Figure~\ref{fig:edit_task} compactly summarises the edit task. The top panel illustrates the scoring pipeline: an \emph{(original CAD program, natural-language instruction)} input pair is consumed by a code LLM, the predicted edited program is rendered, and the score is the IoU between the rendered prediction and the rendered ground-truth target. The middle panels show four representative original/edited pairs and a concrete edit instance with the corresponding CadQuery code (note the parametric coupling between the textually specified \texttt{chamfer\_angle} and the geometric \texttt{chamfer\_drop} that the model must reconstruct). The bottom panel reports the per-model leaderboard across five metrics --- \emph{execute rate}, \emph{strict pass} at IoU $\geq 0.99$, \emph{mean IoU}, \emph{norm}, and \emph{norm}\textsubscript{W} (weighted by edit category) --- including open-source code LLMs (gpt-oss, falconmamba) alongside the GPT-5 family. Two patterns recur: (i) execution rate is uniformly high across models, indicating that syntactic invalidity is rarely the failure mode; and (ii) the strict $\geq 0.99$ pass rate trails the mean IoU substantially, reflecting that models often produce edits that are \emph{geometrically close} but fail the parametric exactness BenchCAD-Edit verification demands.

\input{tables/tab_edit_leakage}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_5.png}
  \caption{\textbf{The edit task: scoring pipeline, qualitative examples, and per-model leaderboard.} \emph{Top:} the BenchCAD-Edit scoring pipeline. Given an \emph{(original CAD query, natural-language instruction)} input pair, the code LLM emits a predicted edited program; we render both the predicted and the ground-truth STEP and score the edit by IoU between the two renders. \emph{Middle-left:} four representative original/edited geometry pairs drawn from the 748-pair BenchCAD-Edit split, illustrating the diversity of dimensional, additive, subtractive, and multi-step edits the benchmark covers. \emph{Middle-right:} a concrete edit instance --- the natural-language instruction (\emph{``At the top of the central pillar, the outer 5\,mm strip of the top surface slopes downward at 30 degrees from horizontal''}) and the corresponding CadQuery program the model must produce, including the parametric expression \texttt{chamfer\_drop = chamfer\_length * math.tan(math.radians(chamfer\_angle))} that links the textually specified angle to the geometric drop. \emph{Bottom:} the BenchCAD-Edit leaderboard across five metrics --- execution rate, strict pass at IoU $\geq 0.99$, mean IoU, norm, and norm$_W$ (weighted by edit category) --- showing that even the strongest reasoning models leave a substantial gap to the strict $\geq 0.99$ verification threshold despite achieving high execution rates.}
  \label{fig:edit_task}
\end{figure*}
